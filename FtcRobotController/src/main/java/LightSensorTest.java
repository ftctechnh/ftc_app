/*import android.content.Context;
import android.hardware.Sensor;
import android.hardware.SensorManager;

/**
 * Created by Kaitlin and Anya on 11/10/15.
 */

public class LightSensorTest //extends //Opmode
{
    /*
    private SensorManager mSensorManager;
    LightSensor beaconColor;

    public void init()
    {
        mSensorManager = (SensorManager) hardwareMap.appContext.getSystemService(Context.SENSOR_SERVICE);
        beaconColor = mSensorManager.getDefaultSensor(Sensor.TYPE_LIGHT);
    }

    public void start()
    {
        mSensorManager.registerListener(this, beaconColor, SensorManager.SENSOR_DELAY_UI);
    }

    public void loop()
    {
        telemetry.addData("beaconColor",beaconColor.getLightDetectedRaw());
    }

    public void stop()
    {

    }
    */
}
